<Project>
    <Import Project="$(MSBuildThisFileDirectory)Directory.Build.local.props"
            Condition="Exists('$(MSBuildThisFileDirectory)Directory.Build.local.props')"/>

    <PropertyGroup>
        <TargetFramework>net10.0</TargetFramework>
        <LangVersion>latest</LangVersion>
        <Nullable>enable</Nullable>
        <ImplicitUsings>enable</ImplicitUsings>
        <Deterministic>true</Deterministic>
        <ContinuousIntegrationBuild Condition="'$(CI)' == 'true'">true</ContinuousIntegrationBuild>
        <AnalysisLevel>latest</AnalysisLevel>
        <!--        <AnalysisMode>AllEnabledByDefault</AnalysisMode>-->
        <EnforceCodeStyleInBuild>true</EnforceCodeStyleInBuild>
    </PropertyGroup>

    <!--#if (db == "TemplateAuthoringOnly") -->
    <PropertyGroup>
        <!--
          Raw template development can override EffectiveDbProvider through Directory.Build.local.props.
          Generated projects materialize only the selected provider and do not need this authoring property.
        -->
        <EffectiveDbProvider Condition="'$(EffectiveDbProvider)' == ''">PostgreSql</EffectiveDbProvider>
    </PropertyGroup>
    <!--#endif -->

    <!--#if (auth == "TemplateAuthoringOnly") -->
    <PropertyGroup>
        <!--
          Raw template development can override EffectiveAuthProvider through Directory.Build.local.props.
          Generated projects materialize only the selected authentication mode and do not need this authoring property.
        -->
        <EffectiveAuthProvider Condition="'$(EffectiveAuthProvider)' == ''">Keycloak</EffectiveAuthProvider>
    </PropertyGroup>
    <!--#endif -->

    <PropertyGroup>
        <IsSourceProject Condition="$([System.String]::Copy('$(MSBuildProjectDirectory)').Replace('\', '/').Contains('/src/'))">true</IsSourceProject>
    </PropertyGroup>

    <PropertyGroup Condition="'$(IsSourceProject)' == 'true'">
        <TreatWarningsAsErrors>true</TreatWarningsAsErrors>
    </PropertyGroup>
</Project>